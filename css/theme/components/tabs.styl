.tabs
    justify-content: center

    &__tab
        border: 0
        color: var(--primary)
        font-weight: bold
        font-size: 20px
        opacity: .6

        @media media-max-lg
            font-size: 14px

        @media media-max-lg
            opacity: 1

        &.on
            font-size: @font-size
            opacity: 1

            @media media-max-lg
                font-size: 14px

    &__content
        border: 0